{
  "name": "openquest",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "packageManager": "bun@1.4.0",
  "type": "module",
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "dev": "vite --configLoader native",
    "build": "bun run --bun tsc --noEmit && bun run --bun vite build --configLoader native",
    "preview": "bun run --bun vite preview --configLoader native --host 127.0.0.1 --port 4173 --strictPort",
    "serve": "bun run migrate:serve && OPENQUEST_PERSIST_PATH=.runtime/state bun run preview",
    "deploy": "bun run build && bun run --bun wrangler deploy",
    "test": "bun test tests",
    "test:watch": "bun test --watch tests",
    "e2e": "bun run --bun playwright test",
    "e2e:live": "bun run --bun playwright test --config playwright.live.config.ts",
    "e2e:ui": "bun run --bun playwright test --ui",
    "lint:oxlint": "oxlint --deny-warnings .",
    "doctor": "react-doctor --yes --no-telemetry --verbose",
    "doctor:design": "react-doctor design --yes --no-telemetry --verbose",
    "migrate:local": "bun run --bun wrangler d1 migrations apply openquest --local",
    "migrate:serve": "bun run --bun wrangler d1 migrations apply openquest --local --persist-to .runtime/state",
    "migrate:remote": "bun run --bun wrangler d1 migrations apply openquest --remote",
    "demo:migrate:local": "bun run migrate:local",
    "demo:seed:local": "bun run --bun wrangler d1 execute openquest --local --file demo/seed.sql",
    "demo:verify:local": "bun scripts/verify-demo-seed.mjs local",
    "demo:setup:local": "bun run demo:migrate:local && bun run demo:seed:local && bun run demo:verify:local",
    "demo:migrate:serve": "bun run migrate:serve",
    "demo:seed:serve": "bun run --bun wrangler d1 execute openquest --local --persist-to .runtime/state --file demo/seed.sql",
    "demo:verify:serve": "bun scripts/verify-demo-seed.mjs serve",
    "demo:setup:serve": "bun run demo:migrate:serve && bun run demo:seed:serve && bun run demo:verify:serve",
    "demo:migrate:remote": "bun run migrate:remote",
    "demo:seed:remote": "bun run --bun wrangler d1 execute openquest --remote --file demo/seed.sql",
    "demo:verify:remote": "bun scripts/verify-demo-seed.mjs remote"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.8.1",
    "@cloudflare/workers-types": "^4.20250829.0",
    "@playwright/test": "^1.52.0",
    "@types/bun": "^1.4.0",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^4.4.1",
    "oxlint": "1.80.0",
    "oxlint-plugin-react-doctor": "0.9.12",
    "oxlint-tsgolint": "7.0.2001",
    "oxspark": "git+ssh://git@github.com/teobucos/oxspark.git#dc25c64017eea2d956de0d9b21c8b9c94c0b48c3",
    "react-doctor": "0.9.12",
    "typescript": "7.0.2",
    "vite": "^6.3.3",
    "webmcp-types": "^0.1.5",
    "wrangler": "^4.4.0"
  }
}
